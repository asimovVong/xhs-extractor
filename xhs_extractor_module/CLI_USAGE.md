# CLI 客户端使用指南

## 🚀 快速开始

### 方式1：交互式模式（推荐）

```bash
python -m xhs_extractor_module.cli
```

然后直接粘贴小红书分享文本即可。

### 方式2：命令行参数

```bash
# 直接提取分享文本
python -m xhs_extractor_module.cli "算法面经... http://xhslink.com/o/ABC123 复制后打开"

# 从URL提取
python -m xhs_extractor_module.cli --url "http://xhslink.com/o/ABC123"

# 启用OCR识别图片文字
python -m xhs_extractor_module.cli --ocr "分享文本..."

# 保存到文件
python -m xhs_extractor_module.cli "分享文本..." > output.txt
python -m xhs_extractor_module.cli --output result.txt "分享文本..."
```

## 📋 完整参数说明

```bash
python -m xhs_extractor_module.cli [选项] [分享文本或URL]

参数:
  input              小红书分享文本或URL（可选，不提供则进入交互式模式）

选项:
  -u, --url          输入的是URL而不是分享文本
  -o, --ocr          启用OCR识别图片中的文字（需要安装paddleocr）
  -i, --images       在输出中包含图片URL列表
  -O, --output FILE  保存完整文本到文件
  -t, --text-only    只输出文本内容，不包含统计信息
  -h, --help         显示帮助信息
```

## 💡 使用示例

### 示例1：基本使用

```bash
python -m xhs_extractor_module.cli "算法面经：字节大模型Agent 11.16 一面： 请介绍 Tran... http://xhslink.com/o/EEfBYaRn4M 复制后打开【小红书】查看笔记！"
```

输出：
```
正在解析分享文本...
正在提取笔记: http://xhslink.com/o/EEfBYaRn4M

================================================================================
📝 笔记内容
================================================================================

【标题】
算法面经：字节大模型Agent 11.16 一面

【正文】
请介绍 Transformer 的架构...

================================================================================
📊 统计信息
================================================================================
笔记ID: abc123
链接: https://www.xiaohongshu.com/explore/abc123
标题长度: 25 字符
正文长度: 500 字符
图片数量: 3
...
```

### 示例2：使用OCR识别图片文字

```bash
python -m xhs_extractor_module.cli --ocr "分享文本..."
```

这会自动识别笔记中图片里的文字，并合并到输出中。

### 示例3：只输出纯文本

```bash
python -m xhs_extractor_module.cli --text-only "分享文本..." > content.txt
```

### 示例4：保存到文件

```bash
python -m xhs_extractor_module.cli --output result.txt "分享文本..."
```

### 示例5：交互式模式

```bash
python -m xhs_extractor_module.cli
```

然后：
1. 粘贴分享文本或输入URL
2. 输入 `ocr` 切换OCR模式
3. 输入 `quit` 退出

## 🔧 前置条件

### 1. 首次登录（只需一次）

```bash
python -m xhs_extractor_module.xhs_login
```

### 2. 安装OCR依赖（可选）

如果需要OCR功能：

```bash
pip install paddleocr paddlepaddle
```

## 📝 输出格式

### 完整输出（默认）

包含：
- 标题
- 正文
- OCR文本（如果启用）
- 统计信息
- 完整文本内容

### 纯文本输出（--text-only）

只输出合并后的文本内容，适合管道操作：

```bash
python -m xhs_extractor_module.cli --text-only "分享文本..." | grep "关键词"
```

## 🎯 常见使用场景

### 场景1：快速提取笔记内容

```bash
# 复制分享文本，然后运行
python -m xhs_extractor_module.cli "粘贴的分享文本"
```

### 场景2：批量处理（结合脚本）

```bash
# 创建脚本 process.sh
#!/bin/bash
while IFS= read -r line; do
    python -m xhs_extractor_module.cli --text-only "$line" >> output.txt
done < links.txt
```

### 场景3：提取后发送给LLM

```bash
# 提取文本
content=$(python -m xhs_extractor_module.cli --text-only "分享文本...")

# 发送给LLM API
curl -X POST https://api.example.com/analyze \
  -d "text=$content"
```

## ⚠️ 注意事项

1. **首次使用需要登录**：运行 `python -m xhs_extractor_module.xhs_login` 进行登录

2. **OCR功能可选**：如果不安装paddleocr，可以正常使用，只是不会识别图片文字

3. **登录态过期**：如果提示需要登录，重新运行登录脚本即可

4. **网络连接**：需要网络连接来访问小红书页面

## 🐛 故障排除

### 问题：提示"未找到登录态文件"

**解决**：运行 `python -m xhs_extractor_module.xhs_login` 进行登录

### 问题：OCR失败

**解决**：
1. 确认已安装：`pip install paddleocr paddlepaddle`
2. 首次使用OCR可能需要下载模型，请耐心等待
3. 如果不需要OCR，可以不使用 `--ocr` 参数

### 问题：提取失败

**解决**：
1. 检查分享文本中是否包含有效链接
2. 检查登录态是否过期（重新登录）
3. 检查网络连接

## 📚 相关文档

- [README.md](README.md) - 模块总体说明
- [QUICKSTART_PLAYWRIGHT.md](QUICKSTART_PLAYWRIGHT.md) - Playwright版本快速开始
- [TESTING.md](TESTING.md) - 测试指南

